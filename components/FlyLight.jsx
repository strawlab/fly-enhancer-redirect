// components/FlyLight.jsx
var React = require('react')

var FlyLight = React.createClass({
  render: function () {
    return (
      <main>
        FlyLight component
      </main>
    )
  }
})

module.exports = FlyLight
